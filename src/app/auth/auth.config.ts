/*
import { PassedInitialConfig } from 'angular-auth-oidc-client';

export const authConfig: PassedInitialConfig = {
  config: {
              authority: 'https://us-east-1pdxlafqqv.auth.us-east-1.amazoncognito.com',
              redirectUrl: typeof window !== 'undefined' ? window.location.origin + '/callback': '',
              postLogoutRedirectUri: typeof window !== 'undefined' ? window.location.origin + '/':'',
              clientId: '29tk0aeiorv1u7st852j7fbh9q',
              scope: 'email openid phone', // 'openid profile offline_access ' + your scopes
              responseType: 'code',
              silentRenew: true,
              useRefreshToken: true,
              renewTimeBeforeTokenExpiresInSeconds: 30,
          }
}
*/


